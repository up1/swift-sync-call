# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'DemoNetwork' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DemoNetwork
  pod 'Alamofire', '~> 3.0'

  target 'DemoNetworkTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DemoNetworkUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
